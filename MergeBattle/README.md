
  # Merge Battle App Design

  This is a code bundle for Merge Battle App Design. The original project is available at https://www.figma.com/design/FI3AoBp12lplnDubuhivgh/Merge-Battle-App-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  